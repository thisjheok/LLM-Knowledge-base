[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kb-mvp"
version = "0.1.0"
description = "Minimal web clipper knowledge base MVP"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["kb_mvp"]
